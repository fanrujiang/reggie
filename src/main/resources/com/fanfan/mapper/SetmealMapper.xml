<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.fanfan.mapper.SetmealMapper">
    <delete id="deleteById">
        delete
        from setmeal
        where id = #{id}
    </delete>

    <select id="findPage" resultType="com.fanfan.dto.SetmealDto">
        select s.*, c.name as 'category_name'
        from setmeal as s
                 inner join category as c on s.category_id = c.id
        <where>
            <if test="name != null and name !='' ">
                and s.name like concat ('%',#{name},'%')
            </if>
        </where>
        order by update_time desc

    </select>
    <select id="getById" resultType="com.fanfan.dto.SetmealDto">
        select s.*, c.name as 'category_name'
        from setmeal as s
                 inner join category as c on s.category_id = c.id
        where s.id = #{id}
    </select>
</mapper>